
const soma = prompt("digite um numero");
const soma2 = prompt("digite outro numero");

 const resultado = Number(soma) + Number(soma2);

alert(`o resultado da soma entre ${soma} + ${soma2} é = ${resultado}`);