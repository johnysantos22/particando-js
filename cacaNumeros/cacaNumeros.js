
function cacaNumeros(_a, b, c) {
    let resultado = 0

    for (let i = a; i <= b; i++) {
        if (a % c === 0 ) {
         resultado [i]
        } 
    }
    return resultado;

}

console.log(cacaNumeros(3, 10, 2));

